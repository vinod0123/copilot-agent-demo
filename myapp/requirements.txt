# Pinned dependencies currently in use by the application.
# Upgraded to address HIGH/CRITICAL CVEs identified by OSV scan.
requests==2.32.3  # was 2.19.1 – fixes CVE-2018-18074 (credential leak via redirect)
flask==3.1.1      # was 0.12   – fixes CVE-2018-1000656 (DoS), CVE-2019-1010083 (DoS), CVE-2023-30861 (session cookie disclosure)
django==4.2.29    # was 2.2    – fixes 20+ CRITICAL/HIGH CVEs (SQL injection, path traversal, account hijack, etc.)
